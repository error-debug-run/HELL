[package]
name = "acm"
version = "0.1.0"
edition = "2024"

[lib]
name = "acm"
crate-type = ["cdylib", "rlib"]  # rlib allows main.rs to link against the lib

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"